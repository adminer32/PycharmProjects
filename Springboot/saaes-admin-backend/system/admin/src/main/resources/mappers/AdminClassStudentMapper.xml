<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saaes.system.admin.classes.mapper.AdminClassStudentMapper">

    <!-- 班级学生分页查询 -->
    <select id="selectClassStudentPage" resultType="com.saaes.system.admin.classes.vo.AdminStudentVO">
        SELECT
        u.id,
        u.code,
        u.name,
        u.telephone,
        cs.create_time as join_time
        FROM class_students cs
        INNER JOIN sys_user u ON cs.user_id = u.id
        <where>
            cs.deleted = 0
            AND u.deleted = 0
            <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
                AND ( ${ew.sqlSegment} )
            </if>
        </where>
    </select>

    <!-- 恢复指定班级的学生关联 -->
    <update id="restoreByClassIds">
        UPDATE class_students SET deleted = 0, update_time = NOW()
        WHERE class_id IN
        <foreach collection="classIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 恢复指定的班级学生关联 (支持批量) -->
    <update id="restoreStudents">
        UPDATE class_students SET deleted = 0, update_time = NOW(), create_time = #{createTime}
        WHERE class_id = #{classId} AND user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
    </update>

    <!-- 查询指定班级的学生关联（包含逻辑删除的数据） -->
    <select id="selectListWithDeleted" resultType="com.saaes.system.client.entity.ClassStudents">
        SELECT * FROM class_students
        WHERE class_id = #{classId} AND user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
    </select>

    <!-- 根据用户ID批量软删除班级学生关联 (级联删除) -->
    <update id="softDeleteByUserIds">
        UPDATE class_students SET deleted = 1, update_time = NOW()
        WHERE user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
    </update>

    <!-- 根据用户ID批量恢复班级学生关联 (级联恢复) -->
    <update id="restoreByUserIds">
        UPDATE class_students SET deleted = 0, update_time = NOW()
        WHERE user_id IN
        <foreach collection="userIds" item="uid" open="(" separator="," close=")">
            #{uid}
        </foreach>
    </update>

    <!-- 物理删除过期的逻辑删除数据 -->
    <delete id="deleteHardExpired">
        DELETE FROM class_students WHERE deleted = true AND update_time &lt; #{threshold}
    </delete>

</mapper>
