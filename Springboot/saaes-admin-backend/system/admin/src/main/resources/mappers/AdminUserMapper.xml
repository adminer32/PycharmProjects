<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saaes.system.admin.user.mapper.AdminUserMapper">

    <!-- 用户分页查询 (连表查询角色信息) -->
    <select id="selectUserPage" resultType="com.saaes.system.admin.user.vo.AdminUserVO">
        SELECT
        u.*,
        r.id as role_id,
        r.name as role_name
        FROM sys_user u
        LEFT JOIN sys_user_job j ON u.id = j.user_id AND j.type = 1 AND j.deleted = false
        LEFT JOIN sys_role r ON j.sys_role_id = r.id
        <where>
            u.deleted = false
            <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
                <if test="ew.nonEmptyOfWhere">
                    AND
                </if>
                ${ew.sqlSegment}
            </if>
        </where>
    </select>

    <!-- 查询已删除的用户 (回收站) -->
    <select id="selectRecyclePage" resultType="com.saaes.system.admin.user.vo.AdminUserVO">
        SELECT
        u.*,
        r.id as role_id,
        r.name as role_name
        FROM sys_user u
        LEFT JOIN sys_user_job j ON u.id = j.user_id AND j.type = 1 AND j.deleted = false
        LEFT JOIN sys_role r ON j.sys_role_id = r.id
        <where>
            u.deleted = true
            <if test="ew != null and ew.sqlSegment != null and ew.sqlSegment != ''">
                <if test="ew.nonEmptyOfWhere">
                    AND
                </if>
                ${ew.sqlSegment}
            </if>
        </where>
    </select>

    <!-- 恢复已删除用户 -->
    <update id="restoreByIds">
        UPDATE sys_user SET deleted = false, update_time = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 物理删除过期的逻辑删除用户 -->
    <delete id="deleteHardExpired">
        DELETE FROM sys_user WHERE deleted = true AND update_time &lt; #{threshold}
    </delete>

</mapper>
