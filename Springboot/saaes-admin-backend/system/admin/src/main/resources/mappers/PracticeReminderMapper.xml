<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saaes.system.admin.practice.mapper.PracticeReminderMapper">

    <!-- 物理删除已读过期的提醒 -->
    <delete id="deleteHardExpired">
        DELETE FROM practice_reminder
        WHERE is_read = 1
        AND IFNULL(update_time, create_time) &lt; #{threshold}
    </delete>

    <!-- 物理删除未读过期的提醒 -->
    <delete id="deleteHardUnreadExpired">
        DELETE FROM practice_reminder WHERE is_read = 0 AND create_time &lt; #{threshold}
    </delete>

    <!-- 物理删除指定任务的提醒 -->
    <delete id="deleteHardByTaskIds">
        DELETE FROM practice_reminder WHERE task_id IN
        <foreach collection="taskIds" item="taskId" open="(" separator="," close=")">
            #{taskId}
        </foreach>
    </delete>

</mapper>
